---
title: ReasonDB - AI-Native Document Intelligence
description: The database that understands your documents. Built for AI agents that need to reason, not just retrieve.
---

**Your documents hold answers. ReasonDB finds them.**

While traditional databases treat documents as data to be indexed, ReasonDB treats them as knowledge to be understood. It's the first database designed from the ground up for how AI actually thinks—navigating hierarchical context, reasoning through complexity, and extracting precise answers from unstructured content.

<Info>
**ReasonDB is not another vector database.** It's a reasoning engine that preserves document structure, enabling AI to traverse your knowledge like a human expert would—scanning summaries, drilling into relevant sections, and synthesizing answers with full context.
</Info>

## The Problem with Current Approaches

AI agents today are crippled by their databases:

| Approach | What It Does | Why It Fails |
|----------|--------------|--------------|
| **Vector DBs** | Finds "similar" chunks | Loses structure. A contract's termination clause isn't "similar" to your question about exit terms—but it's the answer. |
| **RAG Pipelines** | Retrieves then generates | Garbage in, garbage out. Retrieving wrong chunks means wrong answers, no matter how good your LLM. |
| **Knowledge Graphs** | Maps explicit relationships | Requires manual entity extraction. Can't handle the messy reality of real documents. |

**The result?** AI agents that hallucinate, miss critical context, or drown in irrelevant chunks.

## ReasonDB: Intelligence by Design

ReasonDB introduces **Hierarchical Reasoning Retrieval (HRR)**—a fundamentally new architecture where the LLM doesn't just consume retrieved content, it actively navigates your document structure to find exactly what it needs.

<CardGroup cols={3}>
  <Card title="Structure-Aware" icon="tree">
    Documents become navigable trees, preserving the hierarchy that makes complex documents understandable.
  </Card>
  <Card title="LLM-Guided Traversal" icon="brain">
    AI reasons through summaries at each level, choosing which branches to explore—like an expert scanning a document.
  </Card>
  <Card title="Parallel Beam Search" icon="arrows-split">
    Explore multiple promising paths simultaneously. Find answers even when they're buried in unexpected places.
  </Card>
</CardGroup>

## See the Difference

<Tabs>
  <Tab title="Vector DB Approach">
    ```
    Query: "What are the termination conditions?"
    
    → Embed query as vector
    → Find 5 "similar" chunks
    → Hope one contains the answer
    
    Result: Random paragraphs mentioning "termination" 
            scattered across the document. No context.
            LLM hallucinates missing details.
    ```
  </Tab>
  <Tab title="ReasonDB Approach">
    ```sql
    SELECT * FROM contracts
    REASON 'What are the termination conditions?'
    ```

    ```
    → LLM reads document summary
    → Identifies "Section 8: Termination" as relevant
    → Navigates to section, reads subsection summaries
    → Drills into "8.2 Conditions for Termination"
    → Extracts complete answer with full context
    
    Result: Precise answer citing specific clauses,
            with confidence score and reasoning path.
    ```
  </Tab>
</Tabs>

## How It Works

<Steps>
  <Step title="Ingest Your Documents">
    Upload PDFs, Word, Excel, HTML, or plain text. Extractor plugins convert them to Markdown, then ReasonDB builds a hierarchical knowledge tree and generates LLM summaries for every node.
  </Step>
  <Step title="Ask Natural Language Questions">
    Query in plain English. The LLM reads node summaries to navigate the tree intelligently — no keyword matching, actual reasoning.
  </Step>
  <Step title="Get Precise Answers">
    Receive synthesized answers with confidence scores, source citations, and the exact reasoning path taken through your documents.
  </Step>
</Steps>

<Mermaid>
{`flowchart TB
    A[📄 Documents / URLs] --> B[🔌 Extractor Plugin]
    B --> C[📝 Chunk by Structure]
    C --> D[🌳 Build Tree]
    D --> E[✨ LLM Summarizes]
    E --> F[(Knowledge Base)]
    
    G[❓ Your Question] --> H[🧠 LLM Navigates Tree]
    F --> H
    H --> I[🎯 Extract Answer]
    I --> J[✅ Answer + Confidence + Path]`}
</Mermaid>

### Why Trees Scale

With **1 million documents** (50M nodes), a vector DB scans all 50M embeddings. ReasonDB's tree traversal visits **~25 nodes** — the LLM prunes irrelevant branches at each level, acting as an exponential filter. [Learn more](/concepts#scaling-to-millions-of-documents).

## Quick Start

Get from zero to intelligent document search in under 5 minutes:

```bash
# Clone and start
git clone https://github.com/reasondb/reasondb.git && cd reasondb
echo 'REASONDB_LLM_PROVIDER=anthropic' > .env
echo 'REASONDB_LLM_API_KEY=sk-ant-...' >> .env
docker compose up --build
```

Then query your documents with RQL:

```sql
-- Keyword search across a table
SELECT * FROM contracts SEARCH 'payment terms' LIMIT 5

-- LLM-guided reasoning (navigates the document tree)
SELECT * FROM contracts REASON 'What are the late fees and penalties?'

-- Combine filters, search, and reasoning
SELECT * FROM contracts
WHERE tags CONTAINS 'nda' AND metadata.value_usd > 10000
REASON 'What are the termination conditions?'
```

## Built for Production

<CardGroup cols={2}>
  <Card title="RQL Query Language" icon="terminal">
    SQL-like syntax with `SEARCH` (BM25) and `REASON` (LLM) clauses. Familiar yet powerful.
  </Card>
  <Card title="Multi-Provider LLM Support" icon="plug">
    Anthropic, OpenAI, Gemini, Cohere. Switch providers without code changes.
  </Card>
  <Card title="API Key Authentication" icon="shield-check">
    Production-ready security with fine-grained permissions and rate limiting.
  </Card>
  <Card title="Plugin Extraction" icon="puzzle-piece">
    Extensible plugins for PDF, Office, images, audio, URLs. Add your own in Python, Node.js, or Bash.
  </Card>
  <Card title="High Performance" icon="lightning">
    Rust-powered. ACID-compliant. Async parallel traversal. Sub-second responses.
  </Card>
</CardGroup>

## Use Cases

<AccordionGroup>
  <Accordion title="Legal Document Analysis" icon="gavel">
    Navigate complex contracts, find specific clauses, compare terms across agreements. ReasonDB understands document structure—sections, subsections, definitions, exhibits.
  </Accordion>
  <Accordion title="Research & Knowledge Management" icon="graduation-cap">
    Build searchable knowledge bases from papers, reports, and documentation. Ask questions across thousands of documents and get cited answers.
  </Accordion>
  <Accordion title="Customer Support Intelligence" icon="headset">
    Transform support documentation into an AI agent that finds precise answers, not just "related articles."
  </Accordion>
  <Accordion title="Compliance & Policy" icon="shield-check">
    Query policy documents in natural language. Get answers with specific section references and confidence scores.
  </Accordion>
</AccordionGroup>

## Start Building

<CardGroup cols={2}>
  <Card title="5-Minute Quickstart" icon="rocket-launch" href="/quickstart">
    Install, configure, and run your first intelligent search.
  </Card>
  <Card title="Core Concepts" icon="lightbulb" href="/concepts">
    Understand trees, nodes, and hierarchical reasoning.
  </Card>
  <Card title="Plugins" icon="puzzle-piece" href="/guides/plugins">
    Extend extraction with custom plugins.
  </Card>
  <Card title="Tutorials" icon="book-open" href="/tutorials">
    Build real applications: legal search, research assistants, and more.
  </Card>
</CardGroup>

---

<Note>
**Source Available & Extensible**: ReasonDB is free to use, modify, and distribute under the [ReasonDB License](https://github.com/reasondb/reasondb/blob/main/LICENSE). Built with Rust for performance, designed for extension. Star us on [GitHub](https://github.com/reasondb/reasondb).
</Note>
