---
title: Document Ingestion
description: Learn how to ingest documents from text, files, and URLs into ReasonDB
---

ReasonDB supports multiple ingestion methods to import your documents. Each method creates a hierarchical tree structure with LLM-generated summaries for intelligent navigation.

All file and URL extraction is handled by the [plugin system](/guides/plugins). The built-in `markitdown` plugin covers PDF, Office, images, audio, and web pages out of the box.

## Ingestion Overview

<Mermaid>
{`flowchart LR
    subgraph Sources
        Text[Text/Markdown]
        File[PDF/Word/Excel]
        URL[Web Pages/YouTube]
        Chunks[Pre-chunked JSON]
    end

    subgraph Pipeline
        Extract[Extractor Plugin]
        Post[Post-Processor Plugin]
        Chunk[Chunker]
        Tree[Build Tree]
        Summary[LLM Summarize]
    end

    subgraph Storage
        DB[(ReasonDB)]
        Index[BM25 Index]
    end

    Text --> Extract
    File --> Extract
    URL --> Extract
    Chunks -->|bypasses extract + chunk| Tree
    Extract --> Post --> Chunk --> Tree --> Summary --> DB
    Summary --> Index`}
</Mermaid>

## Prerequisites

Before ingesting documents, create a table to organize them:

```bash
curl -X POST http://localhost:4444/v1/tables \
  -H "Content-Type: application/json" \
  -d '{"name": "my_documents"}'
```

The table name is used directly in ingest URLs and RQL queries — no need to save an ID.

## Ingest Text

The simplest method is ingesting plain text or Markdown directly. Text ingestion is **async** — it returns a job ID immediately (HTTP 202) and processes in the background:

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/text \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Project Documentation",
    "content": "# Project Overview\n\nThis project aims to...\n\n## Features\n\n### Feature 1\nDescription...",
    "tags": ["documentation", "internal"],
    "metadata": {"author": "Engineering Team", "department": "Engineering"}
  }'
```

Response (HTTP 202):

```json
{
  "job_id": "job_abc123",
  "status": "queued",
  "created_at": "2026-01-30T10:00:00Z",
  "updated_at": "2026-01-30T10:00:00Z"
}
```

Poll `GET /v1/jobs/{job_id}` until the job completes:

```bash
curl http://localhost:4444/v1/jobs/job_abc123
```

When complete, the result contains the document ID, tree structure, and ingestion stats:

```json
{
  "job_id": "job_abc123",
  "status": "completed",
  "result": {
    "document_id": "doc_xyz789",
    "title": "Project Documentation",
    "total_nodes": 4,
    "stats": { "nodes_created": 4, "summaries_generated": 4 }
  }
}
```

### Parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `title` | Yes | Document title |
| `content` | Yes | Plain text or Markdown content |
| `generate_summaries` | No | Generate LLM summaries (default: true) |
| `tags` | No | Array of tags for filtering |
| `metadata` | No | Custom key-value metadata (e.g., author, department) |

### Markdown Structure

ReasonDB uses Markdown headings to build the tree structure:

```markdown
# Document Title (Root Node)

Introduction paragraph...

## Chapter 1 (Depth 1)

Chapter content...

### Section 1.1 (Depth 2)

Section content...

## Chapter 2 (Depth 1)

More content...
```

<Tip>
Use clear heading hierarchy for better tree structure. Each heading becomes a node with its content and any nested content as children.
</Tip>

## Ingest Files

Upload PDF, Word, Excel, PowerPoint, images, and audio files. The table name goes in the URL path:

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/file \
  -F "file=@/path/to/document.pdf"
```

### Supported Formats

| Format | Extension | Notes |
|--------|-----------|-------|
| PDF | `.pdf` | Text extraction, structure preserved |
| Word | `.docx`, `.doc` | Headers become tree nodes |
| Excel | `.xlsx`, `.xls` | Each sheet as a section |
| PowerPoint | `.pptx` | Each slide as a node |
| Images | `.png`, `.jpg`, `.gif` | OCR text extraction |
| Audio | `.mp3`, `.wav` | Transcription via Whisper |
| HTML | `.html` | Structure from headings |
| Markdown | `.md` | Native support |

<Note>
File ingestion uses the [plugin system](/guides/plugins). The built-in `markitdown` plugin handles format conversion. You can add custom extractor plugins for additional formats.
</Note>

### Example: Ingest a PDF

```bash
curl -X POST http://localhost:4444/v1/tables/research_papers/ingest/file \
  -F "file=@research-paper.pdf"
```

Response (HTTP 202):

```json
{
  "job_id": "job_abc123",
  "status": "queued",
  "created_at": "2026-01-30T10:00:00Z",
  "updated_at": "2026-01-30T10:00:00Z"
}
```

Poll until complete:

```bash
curl http://localhost:4444/v1/jobs/job_abc123
```

## Ingest URLs

Fetch and ingest content from web pages or YouTube videos. URL ingestion is **async**:

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/url \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com/article"
  }'
```

Response (HTTP 202):

```json
{
  "job_id": "job_def456",
  "status": "queued",
  "created_at": "2026-01-30T10:00:00Z",
  "updated_at": "2026-01-30T10:00:00Z"
}
```

Poll `GET /v1/jobs/job_def456` for completion.

<Note>
URL ingestion accepts `url` and `generate_summaries` parameters. Tags and metadata are not supported for URL ingestion — use the text ingestion endpoint if you need those.
</Note>

### Supported URL Types

| Type | Example | Notes |
|------|---------|-------|
| Web Pages | `https://example.com/article` | HTML structure preserved |
| YouTube | `https://youtube.com/watch?v=...` | Transcript extracted |
| GitHub | `https://github.com/org/repo` | README and docs |

### Example: Ingest a YouTube Video

```bash
curl -X POST http://localhost:4444/v1/tables/videos/ingest/url \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://www.youtube.com/watch?v=dQw4w9WgXcQ"
  }'
```

Poll the returned `job_id` for completion. The video transcript will be extracted and organized into a searchable tree.

## Ingest Pre-chunked Content

If you have already split your document into chunks — for example using `pdfplumber`, a custom NLP pipeline, or any other tool — you can ingest them directly via the `/ingest/chunks` endpoint. This **bypasses the extraction and chunking stages entirely** and feeds your content straight into the tree builder and LLM summarizer.

This is useful when:
- You need precise per-chunk metadata (exact page numbers, source line ranges, bounding boxes)
- You are ingesting output from an existing chunking pipeline
- You want full control over how your content is split

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/chunks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Research Paper",
    "description": "Extracted with pdfplumber",
    "chunks": [
      {
        "text": "The mitochondria is the powerhouse of the cell...",
        "heading": "Biology Basics",
        "metadata": {
          "page_number": 1,
          "start_line": 1,
          "end_line": 12,
          "source": "pdfplumber"
        }
      },
      {
        "text": "Photosynthesis converts sunlight into glucose...",
        "heading": "Plant Biology",
        "metadata": {
          "page_number": 2,
          "start_line": 13,
          "end_line": 28
        }
      }
    ],
    "tags": ["biology", "research"],
    "metadata": {"author": "Alice", "year": 2026}
  }'
```

Response (HTTP 202) — same job polling pattern as other endpoints:

```json
{
  "job_id": "job_abc123",
  "status": "queued",
  "created_at": "2026-01-30T10:00:00Z",
  "updated_at": "2026-01-30T10:00:00Z"
}
```

LLM summaries and cross-references are always generated automatically for pre-chunked content.

### Chunk parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `text` | Yes | The text content of this chunk |
| `heading` | No | Heading / title for the chunk — used as the node title in the tree |
| `summary` | No | Pre-computed summary. If provided, the LLM summarization step is skipped for this node |
| `metadata` | No | Free-form JSON object. Well-known keys are promoted to typed fields; all others are stored in `NodeMetadata.attributes` |

### Well-known metadata keys

| Key | Type | Description |
|-----|------|-------------|
| `page_number` | integer | Page number in the source document |
| `start_line` | integer | Line number where this chunk starts in the source file |
| `end_line` | integer | Line number where this chunk ends |
| `section_type` | string | Section type (e.g. `"chapter"`, `"paragraph"`, `"table"`) |

Any other keys you pass in `metadata` are stored as custom attributes on the node and are available for filtering via RQL.

### Providing pre-computed summaries

If your pipeline has already generated summaries (e.g. via a separate LLM call), pass them in the `summary` field to skip LLM generation for that chunk:

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/chunks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "My Doc",
    "chunks": [
      {
        "text": "Full text of the section...",
        "heading": "Introduction",
        "summary": "This section introduces the core concepts.",
        "metadata": {"page_number": 1}
      }
    ]
  }'
```

<Tip>
Mix pre-computed and auto-generated summaries freely. Chunks with a `summary` field use it directly; chunks without one get an LLM-generated summary automatically.
</Tip>

### Body parameters

| Parameter | Required | Description |
|-----------|----------|-------------|
| `title` | Yes | Document title |
| `description` | No | Optional document description |
| `chunks` | Yes | Array of chunk objects (see above) |
| `tags` | No | Array of tags for filtering |
| `metadata` | No | Document-level metadata key-value pairs |

## Metadata and Tags

Add metadata and tags during ingestion for filtering:

```bash
curl -X POST http://localhost:4444/v1/tables/legal/ingest/text \
  -H "Content-Type: application/json" \
  -d '{
    "title": "NDA Agreement",
    "content": "...",
    "tags": ["nda", "confidential", "2024"],
    "metadata": {
      "author": "Legal Team",
      "contract_type": "nda",
      "value_usd": 50000,
      "parties": ["Acme Corp", "Beta Inc"],
      "effective_date": "2024-01-15"
    }
  }'
```

You can then filter by these in RQL queries:

```sql
-- Search within tagged documents
SELECT * FROM legal
WHERE tags CONTAINS ANY ('nda')
SEARCH 'confidentiality clause'

-- Filter by metadata
SELECT * FROM legal WHERE metadata.contract_type = 'nda'

-- Combine metadata filters with reasoning
SELECT * FROM legal
WHERE metadata.contract_type = 'nda'
REASON 'What are the confidentiality obligations?'
```

## Disable LLM Summaries

For faster ingestion without LLM processing:

```bash
curl -X POST http://localhost:4444/v1/tables/my_documents/ingest/text \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Quick Import",
    "content": "...",
    "generate_summaries": false
  }'
```

<Warning>
Disabling summaries significantly reduces search quality since the LLM won't have summaries to navigate by. Only use this for documents you'll query by metadata/tags rather than semantic search.
</Warning>

## Viewing Ingested Documents

After ingestion, you can inspect the document structure:

### List All Documents

```bash
curl http://localhost:4444/v1/documents
```

### Get Document Details

```bash
curl http://localhost:4444/v1/documents/doc_abc123
```

### View Tree Structure

```bash
curl http://localhost:4444/v1/documents/doc_abc123/tree
```

Returns the full hierarchical tree with summaries:

```json
{
  "id": "node_root",
  "title": "Document Title",
  "summary": "This document covers...",
  "content": "Full text content of this node...",
  "depth": 0,
  "is_leaf": false,
  "children": [
    {
      "id": "node_ch1",
      "title": "Chapter 1",
      "summary": "Introduction to...",
      "content": "Chapter content...",
      "depth": 1,
      "is_leaf": true,
      "children": []
    }
  ]
}
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Search Guide" icon="magnifying-glass" href="/guides/search">
    Search your ingested documents
  </Card>
  <Card title="Tables Guide" icon="table" href="/guides/tables">
    Organize documents with tables
  </Card>
  <Card title="Plugin Guide" icon="puzzle-piece" href="/guides/plugins">
    Build custom extractors and processors
  </Card>
  <Card title="Configuration" icon="gear" href="/guides/configuration">
    Docker, environment variables, and production setup
  </Card>
</CardGroup>
